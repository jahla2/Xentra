import {FormEvent,useEffect,useState} from 'react';
import {api,CreateEnvironmentInput,Environment,InvestigationResult} from './api';
const nav=['Overview','Environments','Incidents','Ask AI','Runners','Audit'];
export function App(){
 const[environments,setEnvironments]=useState<Environment[]>([]);const[selected,setSelected]=useState('');const[question,setQuestion]=useState('Why is the API down?');const[result,setResult]=useState<InvestigationResult|null>(null);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const[form,setForm]=useState<CreateEnvironmentInput>({name:'Production Ubuntu',type:'production',connectionType:'runner',runnerUrl:'http://localhost:8090',sshPort:22});
 const refresh=async()=>{try{const items=await api.listEnvironments();setEnvironments(items);if(!selected&&items[0])setSelected(items[0].id)}catch(err){setError((err as Error).message)}};useEffect(()=>{void refresh()},[]);
 const update=(patch:Partial<CreateEnvironmentInput>)=>setForm(current=>({...current,...patch}));
 async function addEnvironment(event:FormEvent){event.preventDefault();setError('');setLoading(true);try{const env=await api.createEnvironment(form);await refresh();setSelected(env.id)}catch(err){setError((err as Error).message)}finally{setLoading(false)}}
 async function investigate(event:FormEvent){event.preventDefault();if(!selected)return;setLoading(true);setError('');setResult(null);try{setResult(await api.investigate(selected,question))}catch(err){setError((err as Error).message)}finally{setLoading(false)}}
 return <div className="shell"><aside className="sidebar"><div className="brand"><span>X</span>Xentra</div><nav>{nav.map((item,i)=><button key={item} className={i===0?'nav active':'nav'}>{item}</button>)}</nav></aside><main className="main"><header><div><p className="eyebrow">DEVOPS COMMAND CENTER</p><h1>Infrastructure overview</h1></div><span className="status">● Control plane</span></header>{error&&<div className="error">{error}</div>}<section className="grid metrics"><article><small>Environments</small><strong>{environments.length}</strong><span>Runner or SSH connected</span></article><article><small>Incidents</small><strong>0</strong><span>No open incidents</span></article><article><small>AI investigations</small><strong>{result?1:0}</strong><span>Current session</span></article></section><section className="grid workbench"><div className="panel"><div className="panel-title"><h2>Environments</h2><span>Secure discovery</span></div>{environments.length===0?<p className="muted">Connect a Xentra Runner or a pinned SSH host.</p>:environments.map(env=><button className={`env ${selected===env.id?'selected':''}`} key={env.id} onClick={()=>setSelected(env.id)}><span className="dot"/><div><b>{env.name}</b><small>{env.hostname||env.sshHost||env.runnerUrl}</small><em>{env.connectionType} · {env.os} · {env.capabilities.join(' · ')||'basic'}</em></div></button>)}<form className="stack" onSubmit={addEnvironment}><input value={form.name} onChange={e=>update({name:e.target.value})} placeholder="Environment name"/><select value={form.connectionType} onChange={e=>update({connectionType:e.target.value as 'runner'|'ssh'})}><option value="runner">Xentra Runner</option><option value="ssh">Ubuntu / Linux SSH</option></select>{form.connectionType==='runner'?<input value={form.runnerUrl??''} onChange={e=>update({runnerUrl:e.target.value})} placeholder="Runner URL"/>:<><input value={form.sshHost??''} onChange={e=>update({sshHost:e.target.value})} placeholder="Host / IP"/><input value={form.sshUser??''} onChange={e=>update({sshUser:e.target.value})} placeholder="SSH username"/><input value={form.sshHostKeyFingerprint??''} onChange={e=>update({sshHostKeyFingerprint:e.target.value})} placeholder="Host key fingerprint (SHA256:...)"/><textarea value={form.sshPrivateKey??''} onChange={e=>update({sshPrivateKey:e.target.value})} placeholder="SSH private key"/><input type="password" value={form.sshPassphrase??''} onChange={e=>update({sshPassphrase:e.target.value})} placeholder="Key passphrase (optional)"/></>}<button className="primary" disabled={loading}>Test & connect environment</button></form></div><div className="panel ai"><div className="panel-title"><h2>Ask Xentra</h2><span>Read-only investigation</span></div><form className="stack" onSubmit={investigate}><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Select environment</option>{environments.map(env=><option value={env.id} key={env.id}>{env.name}</option>)}</select><textarea value={question} onChange={e=>setQuestion(e.target.value)}/><button className="primary" disabled={!selected||loading}>{loading?'Investigating…':'Investigate'}</button></form>{result&&<div className="finding"><div className="confidence">{result.confidence} confidence</div><h3>{result.probableRootCause}</h3><p>{result.summary}</p><b>Recommended action</b><p>{result.recommendedAction}</p><details><summary>Evidence ({result.evidence.length})</summary>{result.evidence.map((item,i)=><pre key={i}>{item.source}{'\n'}{item.output}</pre>)}</details></div>}</div></section></main></div>
}
