export type Environment={id:string;name:string;type:string;connectionType:string;runnerUrl?:string;sshHost?:string;sshPort?:number;sshUser?:string;os:string;hostname:string;capabilities:string[]};
export type InvestigationResult={summary:string;confidence:string;probableRootCause:string;recommendedAction:string;evidence:{source:string;output:string;success:boolean}[]};
export type CreateEnvironmentInput={name:string;type:string;connectionType:'runner'|'ssh';runnerUrl?:string;sshHost?:string;sshPort?:number;sshUser?:string;sshHostKeyFingerprint?:string;sshPrivateKey?:string;sshPassphrase?:string};
const API_URL=import.meta.env.VITE_XENTRA_API_URL??'http://localhost:8080';
async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(`${API_URL}${path}`,{...init,headers:{'Content-Type':'application/json',...(init?.headers??{})}});if(!response.ok){const payload=await response.json().catch(()=>({error:'Request failed'}));throw new Error(payload.error??`Request failed with ${response.status}`)}return response.json() as Promise<T>}
export const api={listEnvironments:()=>request<Environment[]>('/api/environments'),createEnvironment:(input:CreateEnvironmentInput)=>request<Environment>('/api/environments',{method:'POST',body:JSON.stringify(input)}),investigate:(environmentId:string,question:string)=>request<InvestigationResult>('/api/investigations',{method:'POST',body:JSON.stringify({environmentId,question})})};
